import { Default } from 'components/layouts/Default';
import { Game } from 'components/templates/game';

const GamePage = () => {
  return (
    <Default pageName="Game">
      <Game />
    </Default>
  );
};

export default GamePage;
